// ### 5. Combining Arrays

// **Instructions:**

// 1. Create two arrays of your choice.
// 2. Use the concat method to combine the two arrays into a new array.
// 3. Print both the original arrays and the combined array using console.log().

//-----------------

// 1. Create two arrays
const fName = 'Ahad';
const lName = ' Miah';

// 2. Use the concat method to combine the two arrays into a new array.
const fullName = fName + lName;

// 3. Print both the original arrays and the combined array using console.log().
console.log('original array1: ' + fName);
console.log('original array2: ' + lName);
console.log('Combined array: ' + fullName);
